spring:
  application:
    name: service-product
  cloud:
    consul:
      host: consul
      discovery:
        service-name: service-product
    config:
      uri: http://config:8080
  rabbitmq:
    host: rabbitmq