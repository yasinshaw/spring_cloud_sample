spring:
  application:
    name: service-user
  cloud:
    consul:
      host: consul
      discovery:
        service-name: service-user
    config:
      uri: http://config:8080
  rabbitmq:
    host: rabbitmq