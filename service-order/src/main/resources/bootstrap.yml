spring:
  application:
    name: service-order
  cloud:
    consul:
      host: consul
      discovery:
        service-name: service-order
    config:
      uri: http://config:8080
  rabbitmq:
    host: rabbitmq